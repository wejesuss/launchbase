{% extends "layout.njk" %}

{% block header %}
    <section class="hero">
        <div class="presentation">
            <h1>{{home.title}}</h1>
            <p>{{home.presentation}}</p>
        </div>
        <img src="{{home.chef_url}}"/>
    </section>
{% endblock %}

{% block content %}
    <main>
        <h3>Mais acessadas</h3>

        <div class="recipes">
            {% for recipe in recipes %}
                <div id="{{recipe.id}}" class="card">
                    <img src="{{recipe.image}}" alt="{{recipe.title}}">
                    <h5>{{recipe.title}}</h5>
                    <p>por {{recipe.author}}</p>
                </div>
            {% endfor %}

        </div>
    </main>
{% endblock %}