const nodemailer = require("nodemailer")

//  copy this file and fill the fields, save as mailer.js
module.exports = nodemailer.createTransport({
    host: '',
    // address to connect, I'm using mailtrap.io to test => https://mailtrap.io/
    port: 2525,
    // change if necessary
    auth: {
        user: '',
        // user address
        pass: ''
        // password
    }
})